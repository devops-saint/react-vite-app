/**
 * Authentication Hooks
 * Re-export commonly used hooks for convenience
 */

export { useAuth } from './AuthProvider';
export { useMsal, useAccount, useIsAuthenticated } from '@azure/msal-react';
